apiVersion: v1
kind: Service
metadata:
  name: movie-catalog-app
  namespace: movie-catalog
  labels:
    app: movie-catalog-app
spec:
  type: LoadBalancer
  selector:
    app: movie-catalog-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
